[package]
name = "log_processor"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2.3"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

